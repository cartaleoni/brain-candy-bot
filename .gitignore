# Bot state — managed exclusively by GitHub Actions (uses git add -f).
# Ignored locally to prevent code pushes from overwriting GHA state.
posted.json
queue.json
daily_sources.json
pending_review.json
approved.json
rejected_sources.json
training_log.json
discovered_sources.json
pending_discovery_review.json
feed_health.json

# Python
__pycache__/
*.pyc
.env

# OS
.DS_Store
